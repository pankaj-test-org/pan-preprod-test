# pan-preprod-test

## Pipeline Setup

For full pipeline creation notes (Jenkins Multibranch Pipeline, GitHub App permissions, webhook configuration, secrets/variables setup), see the reference repository:

**[marvels-mock README](https://github.com/pankaj-test-org/marvels-mock/blob/main/README.md)**

Apply the same steps to this repo, substituting `pan-preprod-test` for `marvels-mock` and using `https://api.saas-preprod.beescloud.com` as the `CLOUDBEES_API_URL`.